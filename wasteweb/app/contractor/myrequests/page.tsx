// "Create Requests" nav target — reuses the same contractor-scoped requests
// page (contractor sidebar, own requests only, create-request modal) shown
// at /contractor/requests, so both nav entries stay consistent.
export { default } from "../requests/page";
